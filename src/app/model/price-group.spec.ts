import { PriceGroup } from "./price-group";
import { Price } from "./price";

describe("PriceGroup", () => {
  it("should create an instance", () => {
    expect(new PriceGroup([])).toBeTruthy();
  });


});
